"""C-1/C-2 修复 + stale引用清理"""
import re

with open(r'D:\projects\kairos\docs\design\architecture-v1.0.0.md', 'r', encoding='utf-8') as f:
    text = f.read()

# === 0. 修复所有 stale §12.N 引用（来自前序重编号遗漏） ===
stale_refs = {
    '§12.2': '§4',       # 推理皮层
    '§12.1': '§6',       # WM层
    '§12.6': '§7',       # 接入层
    '§12.8': '§10',      # 质量属性
    '§12.9': '§10',      # P6临界余量（现在是§10.9）
    '§12.10': '§10',     # 架构梯度（现在是§10.10）
}

# 精确替换：只替换正文引用，不替换章节标题
# §4 = 推理皮层（新）；§5 = 存储层；§6 = WM；§7 = 接入；§8 = 安全；§9 = 注意力调度器；§10 = 质量属性
for old_ref, new_ref in stale_refs.items():
    # 替换"§12.2"等（在文档正文中）
    count = text.count(old_ref)
    if count > 0:
        text = text.replace(old_ref, new_ref)
        print(f"  {old_ref} → {new_ref} ({count} 处)")

# 修复 §0.1 中的 stale 引用
text = text.replace('结构性记忆守护（§12.2）', '结构性记忆守护（§4）')

# === 1. 修复 L127 重复行 ===
text = text.replace(
    '> 推理皮层（§4）为独立协调层，不入功能栈层编号。注意力调度器（§9）为横切组件，不入功能栈层编号。\n> 推理皮层（§4）为独立协调层，不入功能栈层编号。注意力调度器（§9）为横切组件，不入功能栈层编号。',
    '> 推理皮层（§4）为独立协调层，不入功能栈层编号。注意力调度器（§9）为横切组件，不入功能栈层编号。'
)

# === 2. C-1: 治理层收缩 - 替换 L68 检测器族行 ===
text = text.replace(
    '检测器族：流形曲率/密度监测、分布偏移检测、盲区标注、叙事连贯性检测、VAD分布偏置监测',
    '检测器族（通用监测接口）：结构漂移接口 · 偏置接口 · 健康接口——每个接口接受可插拔 metric 输入（见 §2.2.1 ME-1 子域）'
)

# === 3. C-1: 替换 ME-1 子域中的专用检测器清单 ===
old_me1_detectors = '''**ME-1 监测子域（检测器族）**：
- 职能：实时监测各层运行指标（流形曲率、分布偏移、偏置幅度、健康状态），产出监测数据流供 ME-2 和外部消费
- 通信约束：仅向上/向外输出监测信号（指标、告警、趋势报告），不直接执行治理操作
- 与 ME-2 的接口：检测器族发告警信号至治理器族，治理器族返回回执（ack + severity）。同一检测器在同一时间窗口内的重复告警由治理器族去重
- **叙事连贯性检测器**（新，对应认知基础 §D.1 叙事记忆独立机制约束）：输入——身份注册表中 is_identity 记忆的叙事自洽度时间序列；输出——叙事自洽度趋势（上升/稳定/下降）+ 结构性变化事件（持续下降超阈值触发身份重构提案）。检测器输出直接接入身份注册表的动态建构逻辑（§4 身份注册表——叙事自洽度结构性变化触发身份重评）。此检测器在 v1.0 以降级周期审计模式运行（非连续），每 N 调度周期执行一次全量扫描——与身份建构周期性声明（认知基础 §D.2）一致。v1.1 目标升级为事件驱动模式（叙事自洽度变化超阈即时触发）
- **VAD 分布偏置监测器**（新，对应认知基础 §D.13 VAD 条件激活临时补偿机制）：周期性扫描检索结果集的 VAD 分布与系统整体 VAD 分布的偏差，当低唤醒记忆检索率系统性低于高唤醒且偏差超阈值时发出「情感多样性告警」至策略层语境上下文管理器。告警触发后策略层临时降低情感提升通道的 VAD 匹配度阈值（默认降至原阈值的 70%），使低唤醒记忆获得更高的准入概率
- 与 ME-3 的接口：监测子域不直接访问自观察记忆内容——其感知的治理后效仅通过观测行为级指标（如检索延迟变化、偏置幅度变化）间接获得'''

new_me1 = '''**ME-1 监测子域（三个通用监测接口 + 可插拔 metrics）**：

按信号类型聚合为三个接口，每接口接受可插拔 metric 输入——新增监测指标不需新建组件，注册 metric 到对应接口即可。

**① 结构漂移接口（Structural Drift Interface）**
- 职能：监测系统认知结构的变化——分布偏移、流形曲率、盲区阈值
- metrics（可插拔）：
  - `curvature` — 向量空间流形曲率（was 流形曲率/密度监测器）
  - `distribution_shift` — 标称分布与当前分布偏离加速（was 分布偏移检测器）
  - `blind_spot_coverage` — 盲区覆盖率 = count(never_retrieved)/total（was 盲区标注器）
  - `narrative_coherence` — 身份注册表中 is_identity 记忆的叙事自洽度时间序列趋势（was 叙事连贯性检测器）。输出叙事自洽度趋势（上升/稳定/下降）+ 结构性变化事件——结构性变化触发身份注册表的动态重评（§4 身份注册表）。v1.0 降级为周期审计模式（每 N 调度周期一次），v1.1 升级为事件驱动
- 输出：趋势报告（上升/稳定/下降）+ 结构变化事件（超阈值触发告警至 ME-2）

**② 偏置接口（Bias Interface）**
- 职能：监测系统各层面的偏置漂移——注意力分配、情感分布、使用价值坍缩
- metrics（可插拔）：
  - `attention_wm_bias` — WM 层在注意力调度器分配中获得的占比偏离全周期基线（was 注意力自利检测）
  - `vad_distribution` — 检索结果集的 VAD 分布与系统整体 VAD 分布的偏差，低唤醒记忆检索率系统性低于高唤醒且偏差超阈值时发出情感多样性告警（was VAD 分布偏置监测器）
  - `usage_entropy` — 使用价值分布熵值，持续下降指示注意力过度集中（was 使用分布坍缩检测）
  - `implicit_drift` — 内隐偏向向量与已验证 behavior 的下偏度（was 内隐漂移告警）
- 输出：偏置方向 + 偏移幅度 + 告警（超阈值时发至策略层语境上下文管理器）

**③ 健康接口（Health Interface）**
- 职能：监测系统运行健康度——决策熵、环延迟、P6 合规、校准置信度
- metrics（可插拔）：
  - `decision_entropy` — 元认知层输出稳定性（was 决策熵监测器）
  - `loop_latency` — 环延迟/死锁检测（was 健康计数器）
  - `p6_margin` — P6 压缩比余量（§10.9 P6 临界余量监控）
  - `calibration_confidence_decay` — 外部校准置信度衰减率
- 输出：健康度分数 + 降级信号（触发条件满足时发至宪法主权面）

**接口说明**：
- 通信约束：每个接口向上/向外输出监测信号（指标、告警、趋势报告），不直接执行治理操作
- 与 ME-2 的接口：三个接口发告警信号至治理器族，治理器族返回回执（ack + severity）。同一接口在同一时间窗口内的重复告警由治理器族去重
- 与 ME-3 的接口：监测子域不直接访问自观察记忆内容——其感知的治理后效仅通过观测行为级指标间接获得
- 审计庭的独立感知通道（§10.8 系统事件总线）直接读取三个接口的原始事件流，不依赖接口的预聚合输出'''

text = text.replace(old_me1_detectors, new_me1)

# === 4. C-2: 元数据开销声明 → 添加到 §4 存储层 ===
# 找到存储层 section 中合适的位置
# 在统一LTM描述之后、身份注册表之前
c2_statement = '''
**每条记录元数据清单（v1.0 字段级状态）**：

| 字段 | 类型 | v1.0 状态 | 体积估测 |
|:-----|:-----|:----------|:---------|
| `text` / `content` | 可变长文本 | 生产级 | 主体 |
| `path` | 字符串 (kairos://) | 生产级 | ~50-200 B |
| `embedding` | 浮点向量 | 生产级 | ~768-1536 dim |
| `decontextualization_level` | 浮点数 0-1 | 生产级 | 4 B |
| `encoding_context` | JSON 对象 | v1.0 schema 纳入 | ~200-500 B |
| `calibration_confidence` | 浮点数 0-1 | 生产级 | 4 B |
| `narrative_coherence_score` | 浮点数 0-1 | 生产级 | 4 B |
| VAD 三维 | 3 × 浮点数 0-1 | 生产级（强制录入） | 12 B |
| `is_identity` | 布尔 | 生产级 | 1 B |
| `is_structure` | 布尔 | 生产级 | 1 B |
| `provenance` | 枚举 (exploration/external/etc) | v1.0 新增 | ~10 B |
| 使用权重五维向量 | 5 × 浮点数 | 生产级（影子副本） | 20 B |
| 关系索引 | 可变长图边 | 生产级 | 随连通度增长 |
| **单条估计** | | | **~1.5-3 KB**（含 embedding 约 3-6 KB） |

此清单供存储容量规划参考。实际体积因内容长度和索引密度而异。架构层不预设单条记录体积上界——存储容量按总量规划（ops/configuration.md），非单条规划。'''

# 插入到 §4 存储层——在统一LTM描述后
text = text.replace(
    '**运行期差异检验（§5.3 差异检验）若检测到影子副本写入操作试图跨越隔离防线',
    '每条记录元数据清单（v1.0 字段级状态）\n\n' + c2_statement.strip() + '\n\n**运行期差异检验（§5.3 差异检验）若检测到影子副本写入操作试图跨越隔离防线'
)

# 修复自引用的markdown
text = text.replace(
    '**运行期差异检验（§5.3 差异检验）若检测',
    '**运行期差异检验**（§5.3 差异检验）：若检测'
)

# === 5. 修复其他 stale 引用 ===
# 替换"§0.1/§12.6/§2.2" → "§0.1/§7/§2.2"
text = text.replace('§0.1/§12.6/§2.2', '§0.1/§7/§2.2')

# === 写入 ===
with open(r'D:\projects\kairos\docs\design\architecture-v1.0.0.md', 'w', encoding='utf-8') as f:
    f.write(text)

lines = text.split('\n')
print(f'\n写入完成: {len(lines)} 行')

# 验证
checks = [
    ("stale §12.2 已清除", '§12.2' not in text),
    ("stale §12.6 已清除", '§12.6' not in text),
    ("重复行已修复", text.count('推理皮层（§4）为独立协调层') == 1),
    ("C-1 三接口存在", '结构漂移接口' in text),
    ("C-1 偏置接口存在", '偏置接口' in text),
    ("C-1 健康接口存在", '健康接口' in text),
    ("C-1 metrics 可插拔", '可插拔 metric' in text),
    ("C-2 元数据表存在", '每条记录元数据清单' in text),
    ("C-2 VAD三维列出", 'VAD 三维' in text),
    ("旧叙事检测器引用清除", '叙事连贯性检测器（新' not in text),
    ("旧VAD偏置引用清除", 'VAD 分布偏置监测器（新' not in text),
]

print('\n=== 验证 ===')
for desc, ok in checks:
    print(f"  {'✅' if ok else '❌'} {desc}")
